import React from 'react';

// This component has been deprecated and its features removed.
// It is kept as a placeholder to avoid build errors if referenced elsewhere, 
// though it should be removed from the main App layout.

const VideoWidget: React.FC<any> = () => {
  return null;
};

export default VideoWidget;